// spec: CLS-014
//
// ⚠️ Expected to FAIL compiled, because of C-8.
//
// Naming something that is not a class says so, and names it -- beside the
// existing 'A class can't inherit from itself.'  It used to say 'Only
// instances have properties.', a sentence naming neither the class, nor the
// superclass, nor inheritance, and describing a property access the program
// never wrote: the check asked the superclass for '.ClassName' before the
// comparison meant to reject it could run.

var X := 1;

class C (X);
begin
end
