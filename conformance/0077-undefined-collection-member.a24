// spec: COL-005
//
// ⚠️ Expected to FAIL compiled, because of C-8.
//
// A member a kind does not have.  A Stack has no Get.

var K := Stack ();
K.Push (1);
WriteLn (K.Peek ());

WriteLn (K.Get (0));
