// spec: FUN-007
//
// A method's parameter types ARE enforced, because a method goes through
// overload selection and selection compares whole signatures.

class M;
begin
    function Take (N : Integer); begin Exit N; end
end

WriteLn (M ().Take ('a string'));
