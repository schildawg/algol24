// spec: ENU-003
//
// Members bind as bare names in the enclosing scope, so two enumerations may
// not share a member name -- even though the qualified form would be
// unambiguous.  The collision happens when the members are bound, before any
// use.

type First  = (A, B);
type Second = (A, C);

WriteLn (First.A);
