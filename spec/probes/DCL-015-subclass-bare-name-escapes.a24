// spec: DCL-015, DCL-014
//
// A probe, not a conformance test. It records what the implementation
// DOES; nothing here asserts what it should do.

class P;
private:
    var N : Integer := 1;
begin
end
class Q (P);
begin
    function ByBareName() : Integer;
    begin
        Exit N;
    end
end
WriteLn(Q().ByBareName());
